{
  "__document_status__": "Active",
  "name": "Loopar Authentication",
  "module": "loopar-webpage",
  "doc_structure": "[{\"element\":\"menu_content\",\"data\":{\"label\":\"Authentication\",\"key\":\"menu_content_authentication\",\"id\":\"menu_content_authentication\",\"name\":\"menu_content_authentication\"},\"elements\":[{\"element\":\"div\",\"data\":{\"label\":\"Overview\",\"key\":\"auth_overview\",\"id\":\"auth_overview\",\"name\":\"auth_overview\"},\"elements\":[{\"element\":\"title\",\"data\":{\"key\":\"title_auth_overview\",\"text\":\"Overview\",\"size\":\"md\",\"id\":\"title_auth_overview\",\"name\":\"title_auth_overview\",\"label\":\"Title auth_overview\"}},{\"element\":\"markdown\",\"data\":{\"key\":\"md_auth_overview\",\"value\":\"Loopar ships a complete authentication and authorization layer — no external auth library required. It covers users, roles, granular permissions, sessions, and CSRF protection out of the box.\\n\\n| Piece | Responsibility |\\n|-------|----------------|\\n| **Auth** | Login, logout, session lifecycle |\\n| **PermissionManager** | Resolves what a user is allowed to do |\\n| **ActionScanner** | Discovers controller actions and their access rules |\\n| **PermissionSync** | Keeps permission records in sync with the code |\\n| **CSRF** | Cross-site request forgery protection |\\n\\nPasswords are never stored in plain text — Loopar hashes them with **Argon2** via `loopar.hash(plain)`.\"}}]},{\"element\":\"div\",\"data\":{\"label\":\"Users & Sessions\",\"key\":\"auth_sessions\",\"id\":\"auth_sessions\",\"name\":\"auth_sessions\"},\"elements\":[{\"element\":\"title\",\"data\":{\"key\":\"title_auth_sessions\",\"text\":\"Users & Sessions\",\"size\":\"md\",\"id\":\"title_auth_sessions\",\"name\":\"title_auth_sessions\",\"label\":\"Title auth_sessions\"}},{\"element\":\"markdown\",\"data\":{\"key\":\"md_auth_sessions\",\"value\":\"## The Current User\\n\\nServer-side code reads the authenticated user through `loopar.currentUser` (or `null` when the request is anonymous):\\n\\n```javascript\\nimport { loopar } from \\\"loopar\\\";\\n\\nconst user = loopar.currentUser;\\n\\nif (!user) {\\n  loopar.throw({ code: 401, message: \\\"Authentication required\\\" });\\n}\\n\\nconsole.log(user.name, user.email, user.user_type);\\n```\\n\\n> `loopar.session` is a per-request key/value store (`get`/`set`/`remove`) used for things like list filters — it is **not** the user object.\\n\\n## Looking Up Users\\n\\nUser helpers live on `loopar.auth`:\\n\\n```javascript\\n// Fetch a user by id or email\\nconst user = await loopar.auth.getUser(\\\"user@example.com\\\");\\n\\n// Check whether an account is disabled\\nconst disabled = await loopar.auth.disabledUser(userId);\\n```\\n\\n## Login & Logout\\n\\nAuthentication is handled by the framework's `Auth` layer. A successful login sets an httpOnly **JWT cookie** (`loopar_token_<tenant>`, 1-day expiry, auto-refreshed near expiration) plus a readable CSRF cookie; logout clears them. You normally don't call these directly — the built-in auth pages and API routes do.\"}}]},{\"element\":\"div\",\"data\":{\"key\":\"auth_oauth\",\"id\":\"auth_oauth\",\"name\":\"auth_oauth\"},\"elements\":[{\"element\":\"title\",\"data\":{\"key\":\"title_auth_oauth\",\"id\":\"title_auth_oauth\",\"name\":\"title_auth_oauth\",\"text\":\"OAuth Social Login\",\"size\":\"2xl\"}},{\"element\":\"markdown\",\"data\":{\"key\":\"md_auth_oauth\",\"name\":\"md_auth_oauth\",\"value\":\"Loopar supports social login with **Google** and **GitHub** out of the box (OAuth 2.0; PKCE for Google). When a provider is configured, \\\"Continue with...\\\" buttons appear automatically on the login form.\\n\\n## Configuration\\n\\nTwo ways, per provider — the **Oauth Settings** entity takes precedence over env vars:\\n\\n**Option A — Oauth Settings entity** (Desk → Auth):\\n\\n| Field | Description |\\n|-------|-------------|\\n| `provider` | `google` or `github` |\\n| `client_id` / `client_secret` | From the provider's console |\\n| `scopes` | Optional, space/comma separated (sane defaults per provider) |\\n| `enabled` | Turn the provider on |\\n| `allow_signup` | Create a user on first verified login |\\n\\n**Option B — environment variables** (fallback when no enabled entity row exists):\\n\\n```bash\\nOAUTH_GOOGLE_CLIENT_ID=...\\nOAUTH_GOOGLE_CLIENT_SECRET=...\\nOAUTH_GITHUB_CLIENT_ID=...\\nOAUTH_GITHUB_CLIENT_SECRET=...\\nOAUTH_GOOGLE_ALLOW_SIGNUP=1     # optional\\nOAUTH_BASE_URL=https://mysite.com   # optional, overrides the derived base URL\\n```\\n\\nSet the provider's **redirect URI** to `https://<your-domain>/auth/oauthCallback`.\\n\\n## The Flow\\n\\n```\\nGET /auth/oauth?provider=google\\n  → signed 10-min transaction cookie (state + PKCE verifier)\\n  → redirect to the provider\\n\\nGET /auth/oauthCallback?code=...&state=...\\n  → validate state, exchange code, fetch profile\\n  → require verified email\\n  → link or create the user → login\\n```\\n\\nFailures bounce back to `/auth/login?oauth=<reason>` (e.g. `not_configured`, `email_unverified`, `not_member`, `disabled`).\\n\\n## Account Linking\\n\\n1. **Stable identity first:** a user is matched by `(oauth_provider, oauth_sub)` — so it survives email changes.\\n2. **Email bridge:** on first social login, a user with the same verified email gets the OAuth identity anchored to their account.\\n3. **Signup:** unknown identity + `allow_signup` → a new `Web` user is created (random password, provider picture as avatar). Without `allow_signup`, the login is rejected (`not_member`).\\n\\nSecrets never reach the browser: the login form only asks `POST /auth/oauthProviders`, which returns provider names and labels.\"}}]},{\"element\":\"div\",\"data\":{\"label\":\"Roles & Permissions\",\"key\":\"auth_permissions\",\"id\":\"auth_permissions\",\"name\":\"auth_permissions\"},\"elements\":[{\"element\":\"title\",\"data\":{\"key\":\"title_auth_permissions\",\"text\":\"Roles & Permissions\",\"size\":\"md\",\"id\":\"title_auth_permissions\",\"name\":\"title_auth_permissions\",\"label\":\"Title auth_permissions\"}},{\"element\":\"markdown\",\"data\":{\"key\":\"md_auth_permissions\",\"value\":\"Loopar's authorization model is **role-based with granular, per-document permissions**.\\n\\n## Levels of Control\\n\\n| Level | Example |\\n|-------|---------|\\n| **Document** | Who can read / write / delete `Customer` records |\\n| **Field** | Hide `salary` from non-HR roles |\\n| **Action** | Who can call `actionApprove` on an `Invoice` |\\n\\n## Checking Permissions\\n\\n`PermissionManager` resolves whether a user may perform an action:\\n\\n```javascript\\nimport { PermissionManager } from \\\"loopar\\\";\\n\\n// Can this user delete Customer records?\\nconst allowed = PermissionManager.can(\\\"Customer\\\", \\\"delete\\\", username);\\n\\n// All resolved permissions for a user\\nconst perms = PermissionManager.getPermissions(username);\\n```\\n\\nWhen permissions change, `PermissionManager.invalidate(username)` clears the cached set for that user.\"}}]},{\"element\":\"div\",\"data\":{\"label\":\"Controller Actions & Access\",\"key\":\"auth_actions\",\"id\":\"auth_actions\",\"name\":\"auth_actions\"},\"elements\":[{\"element\":\"title\",\"data\":{\"key\":\"title_auth_actions\",\"text\":\"Controller Actions & Access\",\"size\":\"md\",\"id\":\"title_auth_actions\",\"name\":\"title_auth_actions\",\"label\":\"Title auth_actions\"}},{\"element\":\"markdown\",\"data\":{\"key\":\"md_auth_actions\",\"value\":\"Only methods prefixed with `action` or `publicAction` on a controller are reachable over HTTP. Everything else stays private to the server.\\n\\n```javascript\\nimport { BaseController } from \\\"loopar\\\";\\n\\nexport default class InvoiceController extends BaseController {\\n  // Reachable: GET/POST /api/Invoice/approve — requires login + permission\\n  async actionApprove() { /* ... */ }\\n\\n  // Reachable WITHOUT login: /api/Invoice/webhook\\n  async publicActionWebhook() { /* ... */ }\\n\\n  // NOT reachable over HTTP — internal helper only\\n  recalcTotals() { /* ... */ }\\n}\\n```\\n\\n`ActionScanner` walks every app and registers these actions so `PermissionManager` can guard them. Unauthenticated actions are surfaced through `ActionScanner.getPublicActions()`.\\n\\n## Where Permissions Are Enforced\\n\\nPermission checks run at the **HTTP action layer** (before your `action*` method executes). Server-side code that isn't dispatched through a controller — model hooks, installers, scheduled jobs — is trusted and writes directly:\\n\\n```javascript\\n// No permission check here; this is trusted server code\\nconst doc = await loopar.getDocument(\\\"Invoice\\\", name);\\ndoc.status = \\\"Approved\\\";\\nawait doc.save();\\n```\\n\\n`publicAction*` methods skip login, permission and CSRF checks entirely — validate their input carefully.\"}}]},{\"element\":\"div\",\"data\":{\"label\":\"CSRF & Realtime\",\"key\":\"auth_csrf\",\"id\":\"auth_csrf\",\"name\":\"auth_csrf\"},\"elements\":[{\"element\":\"title\",\"data\":{\"key\":\"title_auth_csrf\",\"text\":\"CSRF & Realtime\",\"size\":\"md\",\"id\":\"title_auth_csrf\",\"name\":\"title_auth_csrf\",\"label\":\"Title auth_csrf\"}},{\"element\":\"markdown\",\"data\":{\"key\":\"md_auth_csrf\",\"value\":\"## CSRF Protection\\n\\nLoopar uses the **double-submit cookie** pattern. On login the server sets a readable `csrf-token` cookie; every state-changing request (non-GET) on an authenticated workspace (`desk`, `api`, `portal`) must echo it back in the `x-csrf-token` header. The framework's client does this automatically; validation runs before the request reaches your controller. `GET` requests and `publicAction*` methods are exempt.\\n\\n## Realtime Authentication\\n\\nLoopar's realtime layer (Socket.IO) authenticates connections with the same **JWT cookie**, scoped to the tenant namespace — a socket for tenant A cannot join tenant B's rooms, so realtime events respect the same tenant isolation as the rest of the framework.\"}}]},{\"element\":\"div\",\"data\":{\"label\":\"Best Practices\",\"key\":\"auth_best\",\"id\":\"auth_best\",\"name\":\"auth_best\"},\"elements\":[{\"element\":\"title\",\"data\":{\"key\":\"title_auth_best\",\"text\":\"Best Practices\",\"size\":\"md\",\"id\":\"title_auth_best\",\"name\":\"title_auth_best\",\"label\":\"Title auth_best\"}},{\"element\":\"markdown\",\"data\":{\"key\":\"md_auth_best\",\"value\":\"- **Never expose the dev site publicly** — it is the admin control center for every tenant.\\n- **Prefer `doc.save()`** over `loopar.db.setValue()` when validation and model hooks matter — the low-level DB API bypasses the model lifecycle.\\n- **Use `action*` for authenticated operations and reserve `publicAction*` for genuinely public endpoints** — public actions skip login, permission and CSRF checks.\\n- **Hash secrets with `loopar.hash()`** (Argon2) — never store credentials or tokens in plain text.\\n- **Scope realtime events** to the tenant namespace; don't broadcast across tenants.\"}}]}]}]",
  "__ENTITY__": "Page Builder"
}
